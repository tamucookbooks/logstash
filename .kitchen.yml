---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04
  - name: centos-6.4

suites:
  - name: agent
    run_list:
      - recipe[logstash::agent]
    attributes:
  - name: server
    run_list:
      - recipe[logstash::server]
    attributes:
